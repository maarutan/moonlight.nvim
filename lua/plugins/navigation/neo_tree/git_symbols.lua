local const = require("utils.constants")

return {
	added = const.icons.git.added,
	modified = const.icons.git.modified,
	deleted = const.icons.git.deleted,
	renamed = const.icons.git.renamed,

	-- Status type
	untracked = const.icons.git.untracked,
	ignored = const.icons.git.ignored,
	unstaged = const.icons.git.unstaged,
	staged = const.icons.git.staged,
	conflict = const.icons.git.conflict,
}
